# Copy to .env, then paste the one-time registration token from
#   Gitea -> Repo -> Settings -> Actions -> Runners -> Create new runner
# Only needed for the FIRST `docker compose up`. After the runner registers,
# its identity is stored in ./data/.runner and this token is ignored (you can
# leave it blank or delete .env).
GITEA_RUNNER_REGISTRATION_TOKEN=
