# Build-time only. The site ships as static HTML served by nginx;
# none of these run in the production image.
pydantic>=2.6
jinja2>=3.1
pyyaml>=6.0
python-frontmatter>=1.1
markdown-it-py>=3.0
linkify-it-py>=2.0
mdit-py-plugins>=0.4
Pygments>=2.17
